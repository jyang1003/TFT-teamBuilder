//routes to go to fav, history, and team build
const express = require('express')
const router = express.Router()
const db = require('../models')

//routes to go to team 
//routes to go to champ info
module.exports = router